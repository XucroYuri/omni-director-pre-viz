---
alwaysApply: false
description: "当开始新任务、评审、或准备交付时"
---

# 工作流与交付（智能生效）

- 开工前先复述：目标/非目标/约束/DoD，并引用 `rules.md` + `docs/roadmap/*`。
- 输入不完整先补问：缺目标、验收、或约束时先补齐再改动。
- 禁止虚构验证：未实际运行不得声称通过。
- 变更最小化：一次任务优先解决一类问题，避免无关大重构。
- 交付可审计：输出必须包含改动清单、验证方式、风险与回滚点。
- 文档同步：改动规则或流程时，同步更新 `rules.md` 与 `.trae/rules/*`。
